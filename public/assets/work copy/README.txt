Drop real project screenshots here with these filenames (referenced from
data/portfolioData.js):

  centralsync.png
  pathwise.png
  ayurveda.png
  lankagrid.png

Until a file exists, each project card automatically falls back to a clean
gradient tile with the project name instead of a placeholder screenshot
that isn't the actual project.
